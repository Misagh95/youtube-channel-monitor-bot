# YouTube Channel Monitor Bot

Monitors YouTube channels via RSS and alerts on new videos.

## Commands

- `/add <channel_id>` - Add channel
- `/remove <channel_id>` - Remove channel
- `/list` - List channels
- `/status` - Status

## Env

See `.env.example`.

## Deploy

Use `Dockerfile` and `render.yaml` for Render.com.
